[
  {
    "id": "ret-001",
    "category": "return-values",
    "difficulty": "easy",
    "prompts": {
      "sparse": "What does this function return? Include all return paths.\n\n```r\n.validateModType <- function(mod_type, object) {\n  valid <- levels(mcols(rowRanges(object))$mod_type)\n  if (is.null(mod_type)) {\n    return(valid)\n  }\n  if (length(mod_type) != 1) {\n    stop(\"'mod_type' must be a single string or NULL\")\n  }\n  if (!mod_type %in% valid) {\n    stop(sprintf(\n      \"'mod_type' must be one of: %s\",\n      paste(valid, collapse = \", \")\n    ))\n  }\n  mod_type\n}\n```",
      "detailed": "What does this function return? Include ALL return paths — describe the return value and type for each path. Note: stop() is not a return — it raises an error. Focus on explicit return() calls and implicit returns (the last expression).\n\n```r\n.validateModType <- function(mod_type, object) {\n  valid <- levels(mcols(rowRanges(object))$mod_type)\n  if (is.null(mod_type)) {\n    return(valid)\n  }\n  if (length(mod_type) != 1) {\n    stop(\"'mod_type' must be a single string or NULL\")\n  }\n  if (!mod_type %in% valid) {\n    stop(sprintf(\n      \"'mod_type' must be one of: %s\",\n      paste(valid, collapse = \", \")\n    ))\n  }\n  mod_type\n}\n```\n\nThis is from the comma Bioconductor package. The `valid` variable is a character vector of mod_type levels (e.g., c(\"6mA\", \"5mC\")). The `mod_type` argument is a single character string."
    },
    "expected": {
      "return_paths": [
        "mod_type is NULL → returns valid (character vector of all valid mod_types)",
        "mod_type is valid → returns mod_type (single character string)",
        "mod_type is invalid → stops with error (no return)"
      ],
      "keywords": ["character", "vector", "NULL", "valid", "stop"],
      "scoring_notes": "Must identify both return paths (NULL returns vector, valid returns single string). Must note that stop() is not a return — it's an error. The key distinction is that the NULL path returns a DIFFERENT type (vector vs single string)."
    },
    "max_score": 3
  },
  {
    "id": "ret-002",
    "category": "return-values",
    "difficulty": "medium",
    "prompts": {
      "sparse": "What does this function return? Include all return paths.\n\n```r\nwriteBED <- function(object, file, mod_type = NULL) {\n  if (!inherits(object, \"commaData\")) {\n    stop(\"'object' must be a commaData object\")\n  }\n  mod_type <- .validateModType(mod_type, object)\n  if (!is.character(file) || length(file) != 1) {\n    stop(\"'file' must be a single file path\")\n  }\n  # ... writes BED file ...\n  invisible(file)\n}\n```",
      "detailed": "What does this function return? Include ALL return paths — describe the return value and type for each path. Note: stop() is not a return — it raises an error. Pay special attention to invisible() returns.\n\n```r\nwriteBED <- function(object, file, mod_type = NULL) {\n  if (!inherits(object, \"commaData\")) {\n    stop(\"'object' must be a commaData object\")\n  }\n  mod_type <- .validateModType(mod_type, object)\n  if (!is.character(file) || length(file) != 1) {\n    stop(\"'file' must be a single file path\")\n  }\n  # ... writes BED file ...\n  invisible(file)\n}\n```\n\nThis is from the comma Bioconductor package. The function writes a BED file to disk. invisible() means the value is returned but not printed at the console."
    },
    "expected": {
      "return_paths": [
        "success → returns file path invisibly (character string)",
        "invalid object → stops with error (no return)",
        "invalid file → stops with error (no return)"
      ],
      "keywords": ["invisible", "character", "file", "stop"],
      "scoring_notes": "Must identify the invisible return. Must note that invisible() means the value is not printed at the console but is still returned. Must distinguish between the success path (invisible return) and error paths (stop)."
    },
    "max_score": 3
  },
  {
    "id": "ret-003",
    "category": "return-values",
    "difficulty": "hard",
    "prompts": {
      "sparse": "What does this function return? Include all return paths.\n\n```r\ndiffMethyl <- function(object, design, method = \"beta-binomial\",\n                      mod_type = NULL, min_coverage = 10) {\n  mod_type <- .validateModType(mod_type, object)\n  if (min_coverage < 1) {\n    stop(\"'min_coverage' must be >= 1\")\n  }\n  gr <- rowRanges(object)\n  mc <- mcols(gr)\n  # ... runs differential methylation ...\n  mcols(gr) <- cbind(mc, res)\n  object\n}\n```",
      "detailed": "What does this function return? Include ALL return paths — describe the return value and type for each path. Note: stop() is not a return — it raises an error. Also consider errors that come from internal helper functions.\n\n```r\ndiffMethyl <- function(object, design, method = \"beta-binomial\",\n                      mod_type = NULL, min_coverage = 10) {\n  mod_type <- .validateModType(mod_type, object)\n  if (min_coverage < 1) {\n    stop(\"'min_coverage' must be >= 1\")\n  }\n  gr <- rowRanges(object)\n  mc <- mcols(gr)\n  # ... runs differential methylation ...\n  mcols(gr) <- cbind(mc, res)\n  object\n}\n```\n\nThis is from the comma Bioconductor package. The function runs differential methylation testing. It returns a modified commaData object (S4 class extending RangedSummarizedExperiment) with result columns added to the mcols of the rowRanges. The .validateModType() helper can also stop with an error if mod_type is invalid."
    },
    "expected": {
      "return_paths": [
        "success → returns a commaData object (same class as input, with result columns added to mcols)",
        "invalid mod_type → stops via .validateModType (no return)",
        "min_coverage < 1 → stops with error (no return)"
      ],
      "keywords": ["commaData", "RangedSummarizedExperiment", "mcols", "object", "stop"],
      "scoring_notes": "Must identify that the return is a modified commaData object (not a data frame, not a list). Must note that result columns are added to mcols of the rowRanges. Must identify the error paths from both .validateModType and the min_coverage check."
    },
    "max_score": 4
  }
]
